# 🔑 GitHub 토큰 권한 확인 가이드

## 권한 오류 해결 방법

### 1. 토큰 권한 확인
1. https://github.com/settings/tokens 접속
2. 생성한 토큰 클릭
3. **repo** 권한이 체크되어 있는지 확인

### 2. 새 토큰 생성 (권장)
1. 기존 토큰 삭제 (선택사항)
2. **Generate new token (classic)** 클릭
3. **Note**: `city2-push`
4. **Expiration**: 90 days (또는 원하는 기간)
5. **Select scopes**: 
   - ✅ **repo** (전체 체크)
     - repo:status
     - repo_deployment
     - public_repo
     - repo:invite
     - security_events
6. **Generate token** 클릭
7. **토큰 복사** (한 번만 보여줌!)

### 3. 푸시 재시도
```bash
git push origin main
```

---

## 또는 SSH 키 사용

SSH 키를 생성하면 토큰 없이 사용할 수 있습니다.

### SSH 키 생성
```bash
ssh-keygen -t ed25519 -C "opunitacitty@gmail.com"
```

### 공개키 등록
1. `type C:\Users\opuni\.ssh\id_ed25519.pub` 실행
2. 출력된 키 복사
3. https://github.com/settings/keys 접속
4. **New SSH key** → 키 붙여넣기

---

**새 토큰을 생성하시거나 SSH 키를 사용하시겠습니까?**

