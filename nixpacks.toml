[phases.setup]
nixPkgs = ["nodejs_18", "python3", "gcc", "gnumake"]

[phases.install]
cmds = [
  "cd backend && npm ci --build-from-source || npm install --build-from-source"
]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "node backend/server.js"

