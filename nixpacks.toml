[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = [
  "cd backend && npm install"
]

[phases.build]
cmds = ["echo 'PostgreSQL Build complete'"]

[start]
cmd = "node backend/server-pg.js"

