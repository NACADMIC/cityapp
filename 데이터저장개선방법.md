# 💾 데이터 저장 개선 가이드

## 🎯 목표:
회원 정보와 비밀번호를 안전하게 저장하고 관리

---

## 📊 현재 상황:

### ❌ 문제점:
```
1. 메모리 DB 사용
   → 서버 재시작 시 모든 데이터 삭제

2. 비밀번호 평문 저장
   → 보안 취약 (해킹 시 그대로 노출)

3. Railway 무료 플랜 제약
   → 파일 저장 불가
```

### ✅ 확인 방법:
```javascript
// backend/database-simple.js
this.users = [];  // 메모리 배열
this.orders = []; // 메모리 배열

// 서버 재시작하면:
this.users = [];  // 다시 빈 배열!
```

---

## 🔧 해결 방법 1: 로컬 서버 + SQLite (추천!)

### 장점:
- ✅ 모든 데이터 파일에 저장
- ✅ 서버 재시작해도 데이터 유지
- ✅ 비밀번호 암호화 가능
- ✅ 무료!
- ✅ 백업 쉬움 (파일 복사)

### 단점:
- ❌ PC 꺼지면 주문 불가
- ❌ 외부 접속은 Cloudflare Tunnel 필요

### 구현 방법:

#### 1단계: bcrypt 설치
```bash
cd backend
npm install bcrypt
```

#### 2단계: database.js 사용
```javascript
// backend/server.js
const db = require('./database');  // database-simple.js → database.js
```

#### 3단계: 비밀번호 암호화
```javascript
// backend/database.js에 이미 구현됨!
const bcrypt = require('bcrypt');

async createUser(phone, name, email, address, password) {
  const hashedPassword = await bcrypt.hash(password, 10);
  // ... DB에 hashedPassword 저장
}
```

#### 4단계: 로컬 서버 실행
```bash
START.bat

외부 접속:
START-EXTERNAL.bat
```

---

## 🔧 해결 방법 2: Railway + PostgreSQL (유료)

### 비용:
- Railway: 월 $5~20
- PostgreSQL: 무료 (Railway 제공)

### 장점:
- ✅ 24/7 운영
- ✅ 데이터 안전
- ✅ 비밀번호 암호화
- ✅ 자동 백업

### 단점:
- ❌ 월 비용 발생
- ❌ 설정 복잡

### 구현 방법:

#### 1단계: Railway PostgreSQL 추가
```
Railway Dashboard
→ New Database
→ PostgreSQL
```

#### 2단계: 환경변수 설정
```
DATABASE_URL=postgresql://...
```

#### 3단계: database-pg.js 사용
```javascript
// backend/server.js
const db = require('./database-pg');
```

---

## 🔧 해결 방법 3: 현재 유지 + 수동 백업

### 장점:
- ✅ 무료
- ✅ 간단

### 단점:
- ❌ 서버 재시작 시 데이터 손실
- ❌ 비밀번호 평문 저장
- ❌ 실제 운영 불가

### 방법:
```javascript
// 주기적으로 데이터 내보내기
console.log('회원 데이터:', JSON.stringify(this.users));
console.log('주문 데이터:', JSON.stringify(this.orders));

// 수동으로 복사해서 보관
```

---

## 💡 추천 방안:

### **단기 (테스트):**
```
현재 메모리 DB 유지
- 빠른 테스트
- 비용 없음
```

### **중기 (실제 운영):**
```
로컬 서버 + SQLite + 비밀번호 암호화
- 안전한 데이터 저장
- 무료
- 영업 시간만 PC 켜두기
```

### **장기 (확장):**
```
Railway + PostgreSQL
- 24/7 운영
- 완벽한 보안
- 자동 백업
- 월 $5~10 투자
```

---

## 🔒 비밀번호 암호화 중요성:

### 평문 저장 (현재):
```javascript
{
  phone: "010-1234-5678",
  password: "mypassword123"  // ← 그대로 보임!
}

해커가 DB 탈취하면:
→ 모든 비밀번호 그대로 노출!
```

### 암호화 저장 (권장):
```javascript
{
  phone: "010-1234-5678",
  password: "$2b$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy"
}

해커가 DB 탈취해도:
→ 비밀번호 알 수 없음!
→ 복호화 불가능!
```

---

## 📝 실행 순서 (로컬 SQLite):

### 1. 백엔드 폴더로 이동
```bash
cd backend
```

### 2. bcrypt 설치
```bash
npm install bcrypt
```

### 3. server.js 수정
```javascript
// const db = require('./database-simple');
const db = require('./database');  // SQLite + 암호화
```

### 4. 기존 DB 파일 삭제 (새로 시작)
```bash
del restaurant.db
```

### 5. 서버 실행
```bash
node server.js
```

### 6. 테스트
```
1. 회원가입
2. 서버 재시작
3. 로그인 시도
4. ✅ 데이터 남아있음!
```

---

## 🎯 결론:

### 지금 당장:
```
✅ 데이터 저장 가능
❌ 서버 재시작하면 사라짐
❌ 비밀번호 평문 저장 위험
```

### 개선 필요:
```
1순위: SQLite + 비밀번호 암호화
2순위: PostgreSQL (유료)
```

### 실제 운영 시:
```
반드시 개선 필요!
- 고객 데이터 보호
- 법적 책임
- 신뢰성 확보
```

---

## ❓ FAQ:

### Q: 지금 회원가입하면 저장됨?
A: 네, 메모리에 저장됩니다. 하지만 서버 재시작하면 삭제됩니다.

### Q: 비밀번호 어떻게 저장됨?
A: 현재는 평문으로 저장됩니다. (위험!)

### Q: 개선하려면?
A: SQLite + bcrypt로 전환하세요. (위 가이드 참고)

### Q: 비용?
A: 로컬 SQLite는 무료입니다!

### Q: 얼마나 걸림?
A: 설정 변경 5분, 테스트 5분 = 총 10분!

---

**로컬 서버 + SQLite + 암호화가 가장 현실적입니다!** 🎯



